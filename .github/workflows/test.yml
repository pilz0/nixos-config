name: build
on:
  push:
    branches: [ "main" ]

jobs:
  debug:
    runs-on: self-hosted
    steps:
      - name: debug
        run: |
          ls
          ls nixos-config
  build:
    runs-on: self-hosted
    steps:
      - name: build with colmena
        run: |
          nix run nixpkgs#colmena -- build --impure