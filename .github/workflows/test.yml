name: build
on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: build with colmena
        run: |
          ls
          nix run nixpkgs#colmena -- build --impure